#include <string>
#include <iostream>
#include <vector>
#include <queue>

void find_adjacent_vertices(int current_row, int current_col, int num_rows, int num_col,  int pos_value);

std::vector<std::vector<bool>> empty_visited_matrix (int num_rows, int num_cols);